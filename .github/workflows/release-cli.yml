name: Publish CLI

on:
  workflow_dispatch:
    inputs:
      publish:
        description: Publish the verified package to npm
        required: true
        default: false
        type: boolean
      dist_tag:
        description: npm dist-tag
        required: true
        default: latest
        type: choice
        options:
          - latest
          - next

permissions:
  contents: read
  id-token: write

jobs:
  verify-and-publish:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: npm

    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
      - run: corepack enable
      - run: corepack pnpm install --frozen-lockfile
      - run: corepack pnpm check
      - run: corepack pnpm smoke:generated
      - name: Build package without publishing
        if: ${{ !inputs.publish }}
        run: npm pack --dry-run ./create-ts-fastify-business-starter
      - name: Publish immutable CLI package
        if: ${{ inputs.publish }}
        run: >-
          npm publish ./create-ts-fastify-business-starter
          --access public
          --provenance
          --tag "${{ inputs.dist_tag }}"
